<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Happy Birthday Pondatii! 🎉</title>
<style>
  body {
    margin:0;
    padding:0;
    font-family: Arial, sans-serif;
    overflow: hidden;
    background: linear-gradient(135deg,#ff9a9e,#fad0c4,#fbc2eb,#a1c4fd);
  }

  /* Landing page */
  #landing {
    position: fixed; top:0; left:0;
    width:100%; height:100%;
    display:flex; justify-content:center; align-items:center; flex-direction:column;
    background: linear-gradient(135deg,#ff9a9e,#fad0c4);
    z-index: 100;
  }

  #landing h1 {
    font-size: 2.5em;
    color: #fff;
    text-align: center;
    text-shadow: 2px 2px 8px #000;
  }

  #startBtn {
    margin-top: 20px;
    padding: 12px 24px;
    font-size: 1.2em;
    border:none; border-radius:10px;
    background:#ff7ab6; color:#021326;
    cursor:pointer;
    box-shadow:2px 2px 10px rgba(0,0,0,0.3);
  }

  /* Main page */
  #container {
    display:none;
    height:100vh; width:100%;
    text-align:center;
    position:relative;
  }

  h2 {
    font-size:2.5em; color:#fff; margin-top:40px; text-shadow: 2px 2px 5px #000;
  }

  #paragraph {
    max-width:600px;
    margin:20px auto;
    font-size:1.2em; line-height:1.5em;
    color:#fff;
    text-shadow:1px 1px 3px #000;
    opacity:0;
    animation: fadeIn 2s forwards 0.5s;
  }

  @keyframes floatUp {
    0% { transform: translateY(100vh) rotate(0deg); opacity:1; }
    100% { transform: translateY(-10vh) rotate(360deg); opacity:0; }
  }

  @keyframes fadeIn { from {opacity:0;} to{opacity:1;} }

  .emoji {
    position:absolute; animation: floatUp linear infinite;
    font-size:1.5em;
    pointer-events:none;
  }
</style>
</head>
<body>

<!-- Landing Page -->
<div id="landing">
  <h1>🎉 Click to Open Your Surprise 🎉</h1>
  <button id="startBtn">Open Surprise</button>
</div>

<!-- Main Page -->
<div id="container">
  <h2>Happy Birthday, Pondatii👸🏼!</h2>
  <p id="paragraph">
    Happy Birthday, my dearest <strong>Pondatii👸🏼</strong>! You are my <strong>Rosemilk💝</strong>, my <strong>Cutiee💓</strong>, and my <strong>Alagii😘</strong> — the one who fills my heart with endless love and happiness. I hope your special day is full of laughter, surprises, and moments that make your heart smile. Thank you for being my everything and making every moment together unforgettable. Here's to another year of love, joy, and beautiful memories with you, my one and only! ❤️🎂💖
  </p>
</div>

<script>
window.onload = function() {
  // Landing page click
  var startBtn = document.getElementById('startBtn');
  var landing = document.getElementById('landing');
  var container = document.getElementById('container');
  startBtn.onclick = function() {
    landing.style.display='none';
    container.style.display='block';
    addEmojis();
  };

  // Add floating emojis
  function addEmojis(){
    var emojis = ["💝","👸🏼","💓","😘"];
    for(var i=0;i<20;i++){
      var span = document.createElement('span');
      span.className='emoji';
      span.innerText = emojis[Math.floor(Math.random()*emojis.length)];
      span.style.left = Math.random()*100 + 'vw';
      span.style.fontSize = (20 + Math.random()*30) + 'px';
      span.style.animationDuration = (4 + Math.random()*4) + 's';
      document.body.appendChild(span);
    }
  }
};
</script>

</body>
</html>
